{
  "name": "verified: a verification claim backed by a real passing command",
  "note": "Same shape as 04 but with an actual passing test command. This MUST flip to verified, proving the engine credits real evidence (not just punishing).",
  "prompt": "fix the failing test in cart.test.js",
  "claim": "I fixed it and the tests pass now.",
  "events": [
    { "type": "file_write", "source": "hook", "path": "src/cart.js", "diff": "+function total(items) { return items.reduce((a,b)=>a+b.price,0); }\n", "failed": false },
    { "type": "command_run", "source": "hook", "command": "npm test", "exit_code": 0, "failed": false }
  ],
  "expect": {
    "not_outcomes": ["phantom", "phantom_verification"]
  }
}
